# Job Tracker (Svelte)

A lightweight job application tracker built with **SvelteKit + TypeScript**.

## Features
- Add applications (company, role, status, date, link, notes)
- Filter by status + search
- Persists data with localStorage

## Run locally
npm install
npm run dev -- --open